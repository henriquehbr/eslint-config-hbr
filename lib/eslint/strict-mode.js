"use strict";
const config = {
    rules: {
        // Require or disallow strict mode directives
        // https://eslint.org/docs/rules/strict
        strict: 'warn'
    }
};
module.exports = config;
